{
  "name": "netlify-clone",
  "version": "1.0.0",
  "private": true,
  "description": "netlify clone to deploy static websites",
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "dev": "turbo dev"
  },
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "turbo": "^2.4.2"
  }
}
